enum USER_ROLE {
  manager = "admin",
  conflictManager = "conflict",
  registrationManager = "registration",
}

enum STORAGE_KEY {
  token = "token",
  role = "role",
  lang = "i18nextLng",
  theme = "theme",
}

export { USER_ROLE, STORAGE_KEY };
